name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, closed]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test
        env:
          DATABASE_PATH: ':memory:'

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test]
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build --if-present

      - name: Verify application starts
        run: |
          timeout 30 node src/index.js &
          APP_PID=$!
          # Wait for the app to start listening
          for i in $(seq 1 10); do
            sleep 2
            if curl --fail --silent http://localhost:${PORT}/health; then
              echo "App started successfully"
              kill $APP_PID 2>/dev/null || true
              exit 0
            fi
          done
          echo "App failed to start within timeout"
          kill $APP_PID 2>/dev/null || true
          exit 1
        env:
          PORT: 3000
          DATABASE_PATH: ':memory:'

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ github.sha }}
          path: |
            dist/
            package.json
            package-lock.json
          retention-days: 7

  deploy-preview:
    name: Deploy Preview
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'pull_request' && github.event.action != 'closed'
    timeout-minutes: 10
    environment:
      name: preview
      url: ${{ steps.deploy.outputs.preview_url }}
    steps:
      - name: Validate required secrets
        run: |
          if [ -z "$DEPLOY_TOKEN" ]; then
            echo "ERROR: DEPLOY_TOKEN secret is not set. Skipping deployment."
            exit 1
          fi
          if [ -z "$DATABASE_URL" ]; then
            echo "ERROR: PREVIEW_DATABASE_URL secret is not set. Skipping deployment."
            exit 1
          fi
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
          DATABASE_URL: ${{ secrets.PREVIEW_DATABASE_URL }}

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Deploy to preview environment
        id: deploy
        # TODO: Replace with actual deployment command, e.g.:
        # - Railway: railway up --environment preview-${{ github.event.pull_request.number }}
        # - Vercel:  vercel deploy --token $VERCEL_TOKEN
        # - Render:  trigger deploy hook via curl
        run: |
          echo "preview_url=https://preview-pr-${{ github.event.pull_request.number }}.example.com" >> "$GITHUB_OUTPUT"
          echo "Deploying PR #${{ github.event.pull_request.number }} to preview environment"
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
          DATABASE_URL: ${{ secrets.PREVIEW_DATABASE_URL }}

      - name: Comment preview URL on PR
        uses: actions/github-script@v7
        with:
          script: |
            const previewUrl = '${{ steps.deploy.outputs.preview_url }}';
            const body = `### Preview Deployment\n\n✅ Preview deployed successfully!\n\n**URL:** ${previewUrl}\n\n*This preview will be cleaned up when the PR is closed.*`;
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const existingComment = comments.find(c => c.body.includes('### Preview Deployment'));
            if (existingComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existingComment.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

  cleanup-preview:
    name: Cleanup Preview
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    steps:
      - name: Validate required secrets
        run: |
          if [ -z "$DEPLOY_TOKEN" ]; then
            echo "ERROR: DEPLOY_TOKEN secret is not set. Skipping cleanup."
            exit 1
          fi
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}

      - name: Tear down preview environment
        # TODO: Replace with actual teardown command for your platform, e.g.:
        # railway environment delete preview-${{ github.event.pull_request.number }}
        run: |
          echo "Cleaning up preview environment for PR #${{ github.event.pull_request.number }}"
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}

      - name: Comment cleanup on PR
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '### Preview Deployment\n\n🧹 Preview environment has been cleaned up.',
            });

  deploy-production:
    name: Deploy Production
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    timeout-minutes: 10
    environment:
      name: production
      url: ${{ steps.deploy.outputs.production_url }}
    steps:
      - name: Validate required secrets
        run: |
          if [ -z "$DEPLOY_TOKEN" ]; then
            echo "ERROR: DEPLOY_TOKEN secret is not set. Cannot deploy to production."
            exit 1
          fi
          if [ -z "$DATABASE_URL" ]; then
            echo "ERROR: PRODUCTION_DATABASE_URL secret is not set. Cannot deploy to production."
            exit 1
          fi
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
          DATABASE_URL: ${{ secrets.PRODUCTION_DATABASE_URL }}

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Deploy to production
        id: deploy
        # TODO: Replace with actual production deployment command, e.g.:
        # - Railway: railway up --environment production
        # - Vercel:  vercel deploy --prod --token $VERCEL_TOKEN
        # - AWS:     aws ecs update-service --cluster prod --service app --force-new-deployment
        run: |
          echo "production_url=https://app.example.com" >> "$GITHUB_OUTPUT"
          echo "Deploying commit ${{ github.sha }} to production"
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
          DATABASE_URL: ${{ secrets.PRODUCTION_DATABASE_URL }}
          PORT: ${{ secrets.PRODUCTION_PORT }}

      - name: Post-deploy health check
        id: healthcheck
        run: |
          # TODO: Replace PRODUCTION_URL with the actual deployed URL once real deployment is configured.
          # Until then, this step will fail on placeholder URLs — which is the safe default.
          PRODUCTION_URL="${{ steps.deploy.outputs.production_url }}"
          if [ "$PRODUCTION_URL" = "https://app.example.com" ]; then
            echo "WARNING: production_url is still a placeholder. Skipping health check."
            echo "Replace the deploy step with actual deployment commands before enabling health checks."
            exit 0
          fi
          MAX_RETRIES=5
          RETRY_DELAY=10
          for i in $(seq 1 $MAX_RETRIES); do
            echo "Health check attempt $i/$MAX_RETRIES..."
            HTTP_STATUS=$(curl --silent --output /dev/null --write-out "%{http_code}" "${PRODUCTION_URL}/health" || echo "000")
            if [ "$HTTP_STATUS" = "200" ]; then
              echo "Health check passed (HTTP $HTTP_STATUS)"
              exit 0
            fi
            echo "Health check failed (HTTP $HTTP_STATUS), waiting ${RETRY_DELAY}s..."
            sleep $RETRY_DELAY
          done
          echo "Health check failed after $MAX_RETRIES attempts — triggering rollback"
          exit 1

      - name: Rollback on health check failure
        if: failure() && steps.healthcheck.outcome == 'failure'
        # TODO: Replace with actual rollback command for your platform, e.g.:
        # - Railway: railway rollback --environment production
        # - AWS ECS: aws ecs update-service --cluster prod --service app --task-definition <previous>
        run: |
          echo "Rolling back production deployment due to failed health check"
          echo "WARNING: No real rollback configured — manual intervention required!"
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}

      - name: Notify on deployment failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: 'failure',
              description: 'Production deployment failed or was rolled back',
              context: 'deploy/production',
            });
